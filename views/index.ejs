<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>all post</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>qura post</h1>
    <% for (post of posts) { %>
       <div class="post">
        <h3 class="user"> @<%= post.usearname %></h3>
        <h4 class="content"><%= post.content %></h4>
        <br>
        <a href="http://localhost:2000/posts/<%= post.id  %>">see in deltaies</a>&nbsp&nbsp
        <a href="http://localhost:2000/posts/<%= post.id  %>/edit">edit</a>
        <br>
        <form method="post" action="/posts/<%= post.id  %>?_method=DELETE">
            <button>delete buttton</button>
        </form>
        
       </div>
   <% } %>
   <br>
   <br>
   <a href="http://localhost:2000/posts/new"> creat new post</a>
</body>
</html>